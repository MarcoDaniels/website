import * as React from 'react'

export const AddressBar: React.SFC = () => {

    return (
        <div className="address-bar">
            This is address bar
        </div>
    )
}

export default AddressBar